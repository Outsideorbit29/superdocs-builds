# Core runtime — keep it light: the engine works fully offline.
httpx>=0.27          # SuperDocs API client
pypdfium2>=5        # PDF text extraction (evidence pile)
python-docx>=1.1    # .docx text extraction (evidence pile)

# Tests
pytest>=8

# Optional: the CrewAI cover-writer crew (auto-disabled when absent).
# crewai>=1.15
# crewai[google-genai]
